{
  "name": "food-quality-manager",
  "version": "1.0.0",
  "description": "Food Quality Manager - International Food Pivot Srl (React + Node.js/Express + MySQL)",
  "main": "server/app.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/app.js",
    "server": "node server/app.js",
    "server:dev": "nodemon server/app.js",
    "dev": "nodemon server/app.js",
    "db:init": "node server/initDb.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "mysql2": "^3.23.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
